name = "ordsamling"
compatibility_date = "2026-04-04"
pages_build_output_dir = "dist"

# Secret environment variables (set in Cloudflare Pages dashboard, never committed):
#   GOOGLE_CLIENT_ID     — OAuth 2.0 client ID (also set as VITE_GOOGLE_CLIENT_ID at build time)
#   GOOGLE_CLIENT_SECRET — OAuth 2.0 client secret (server-side only, never sent to browser)
